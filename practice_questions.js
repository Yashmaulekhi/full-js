/*Write a function to count how many times each element appears in an array.

Write a function to check if two strings are anagrams.

Reverse a string without using .reverse().

Find the longest word in a sentence.

Given an array of numbers, return a new array with only unique numbers.

Given a string, count how many vowels it has.

Check if a string is a palindrome.

Merge two sorted arrays into one sorted array.

Find the second largest number in an array.

Given an array, move all zeros to the end while keeping the order.

Math & Number Logic

Check if a number is prime.

Find the factorial of a number (without recursion).

Generate Fibonacci series up to N numbers.

Find the GCD (Greatest Common Divisor) of two numbers.

Write a function to find the sum of all digits of a number.

Object & Data Logic

Convert an array of objects into a single object grouped by a key.

Count how many properties an object has.

Compare two objects to check if they are identical.

Convert an object into an array of key-value pairs.

Deep clone an object without using JSON methods.

Function & Algorithm Logic

Implement a custom map() method.

Implement a custom filter() method.

Implement a custom reduce() method.

Write a function that returns n-th largest element from an array.

Implement a function to flatten a nested array.

Logical Puzzles

Given two arrays, find common elements.

Given student scores, return the top 3 highest scorers.

Check if an array has any duplicates.

Write a function to rotate an array by k positions.

Remove falsy values (false, 0, "", null, undefined, NaN) from an array.*/
